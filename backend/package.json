{
  "name": "kulibayar-backend",
  "version": "1.0.0",
  "type": "module",
  "description": "KuliBayar Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "for file in $(find . -name \"*.js\" -not -path \"./node_modules/*\"); do node --check \"$file\" || exit 1; done",
    "test": "echo \"Tests: Run forge test in root directory\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.1",
    "exif-parser": "^0.1.12",
    "express": "^4.21.0",
    "express-rate-limit": "^8.5.2",
    "helmet": "^7.1.0",
    "multer": "^1.4.5-lts.1",
    "siwe": "^2.1.4",
    "tesseract.js": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
